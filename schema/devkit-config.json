{
  "$defs": {
    "AppConfig": {
      "description": "One runnable app. `base_port` and `launch` are required; `path` is required\ntoo whenever `defaults.doppler_yaml` cannot infer the directory.\n\n```toml\n[apps.api]\nbase_port    = 9100\nlaunch       = [\"doppler\", \"run\", \"-c\", \"dev_local\", \"--\", \"nitro\", \"dev\", \"--port\", \"{{ port }}\"]\nurl_env      = \"API_BASE_URL\"\nprovides_url = true\nstatic_env   = { SOME_JWT_SECRET = \"local-dev-placeholder-value\" }\n\n[apps.web]\nbase_port = 4100\npath      = \"apps/web\"\nlaunch    = [\"next\", \"dev\", \"-p\", \"{{ port }}\"]\nurl_env   = \"API_BASE_URL\"\n```\n\n`web` names the provider through `url_env` rather than by port, so the two\napps stay wired together in every worktree.",
      "properties": {
        "base_port": {
          "description": "Start of the app's port band. Each worktree is allocated its own port\nfrom here by the registry, so two worktrees never collide.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "launch": {
          "description": "The complete launch command as one argv, run verbatim — devkit builds no\nprefix of its own, so any `doppler run -c <config> --` wrapper belongs\nhere. Rendered as minijinja over `{{ port }}`, `ports['<app>']`, and\n`[templates.variables]`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path": {
          "default": null,
          "description": "App subdirectory relative to the repo root, when it differs from the\napp's own name. Required unless `defaults.doppler_yaml` infers it.",
          "type": [
            "string",
            "null"
          ]
        },
        "prep_files": {
          "default": [],
          "description": "Files written into the app's directory during `issue setup` (before\n`setup`).",
          "items": {
            "$ref": "#/$defs/PrepFile"
          },
          "type": "array"
        },
        "provides_url": {
          "default": false,
          "description": "This app serves the URL that consumer apps wire to via their `url_env`.\nExactly one app (the API) is normally marked; consumers reference it by\nrole, not by a hardcoded name.",
          "type": "boolean"
        },
        "setup": {
          "default": [],
          "description": "Commands run in the app's directory during `issue setup`, in order. Each\ninner array is one argv (program + args), e.g.\n`[[\"doppler\",\"run\",\"-c\",\"local\",\"--\",\"bun\",\"install\"]]`.",
          "items": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "type": "array"
        },
        "static_env": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Env vars always set for this app, rendered as minijinja over the same\nvariables as `launch`. The lowest layer: a task's `env` and the CLI's\n`--env` both win over it.",
          "type": "object"
        },
        "url": {
          "default": null,
          "description": "Address the app serves on, as a template over the same variables as\n`launch` (`{{ port }}`, `ports['<app>']`, `[templates.variables]`).\nDefaults to `DEFAULT_APP_URL`; set it for an app that serves https, a\ncustom host, or a path prefix — devkit never terminates TLS itself.",
          "type": [
            "string",
            "null"
          ]
        },
        "url_env": {
          "default": null,
          "description": "Env var this app receives the `provides_url` app's URL through.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "base_port",
        "launch"
      ],
      "type": "object"
    },
    "AppMatch": {
      "description": "`[harness.app_match]`: how the guard turns a hint into an app name.\n\nExact-name, exact-path and path-under-path matching are unconditional and\ntake no configuration. This table tunes only the fuzzy rung that runs when\nnone of those resolve, which is the one place the guard guesses.\n\n`#[serde(default)]` sits on the container so a layer naming one key inherits\nthe other two rather than zeroing them.\n\n```toml\n[harness.app_match]\nfuzzy     = false   # name no app rather than guess one\nmin_score = 75\n```",
      "properties": {
        "fuzzy": {
          "default": true,
          "description": "Run the fuzzy matcher at all. `false` stops after exact and path\nmatching, so an unrecognised hint names no app and the guard falls back\nto `devkit config apps`.",
          "type": "boolean"
        },
        "max_typos": {
          "default": 1,
          "description": "Substitutions, insertions and deletions the matcher forgives.\n\nOne is what separates `lab-tools` from an app declared `lab_tools`.\n`frizbee::Config::default()` allows zero, which filters exactly that\ncase, so this default is devkit's rather than the library's. Raising it\nbuys confidently wrong app names.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "min_score": {
          "default": 60,
          "description": "Below this score no app is named. Pointing an agent at another app's\nserver is worse than naming none.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "BriefConfig": {
      "description": "What `devkit brief` emits. All default on: the hooks ship enabled and\nconfig decides whether they produce anything, so turning the output off is\none line rather than a hook-wiring task. A section with nothing to report\nis omitted whatever its switch says; a switch turned off suppresses the\nsection even when the checkout has something to put in it.\n\n```toml\n[brief]\nlocks = false\npins  = false\n```",
      "properties": {
        "apps": {
          "default": true,
          "description": "The apps line and the `devrun up` / `portm` bullets. Live servers this\nworktree holds are reported regardless: a bound port is a fact about\nthe machine, not a listing the brief chose to carry.",
          "type": "boolean"
        },
        "enabled": {
          "default": true,
          "description": "The whole brief.",
          "type": "boolean"
        },
        "locks": {
          "default": true,
          "description": "The `lockm` line. Whether sessions ever share this checkout is not\nsomething devkit can observe, so it is a switch rather than a probe.",
          "type": "boolean"
        },
        "pins": {
          "default": true,
          "description": "The library-versions section.",
          "type": "boolean"
        },
        "tasks": {
          "default": true,
          "description": "The task table and the `devrun task` bullet.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "CommandRule": {
      "description": "One `[harness.commands.<name>]` entry: a set of programs whose invocation\nthe guard refuses, and the correction it offers instead.\n\nDeliberately not a regex. Parsing the command, unwrapping wrappers and\nrunners, and removing a program's own global options (`git -C`) are\ndevkit's job; a rule that had to restate them would get them wrong.\n\n```toml\n[harness.commands.git-worktree]\nprograms = [\"git\"]\nargs     = [\"worktree\", \"add\"]\nreason   = \"use `issue setup <id>`, which records the worktree\"\n\n[harness.commands.nitro-dev]\nprograms = [\"bun\", \"node\"]\nargs     = [\"**\", \"*nitro\", \"dev\"] # bun --cwd . nitro dev, node node_modules/.bin/nitro dev\nreason   = \"start the api with `devrun up api`\"\n```\n\n`args` matches the typed arguments after the program's own global options\nare removed, so `git -C /repo worktree add` fires the first rule and\n`git worktree list` does not. In the second rule, `**` skips any flags\nbefore the server and `*nitro` matches the binary at any path, so it\nrefuses `bun --filter=api nitro dev` and allows `bun nitro build`.",
      "properties": {
        "action": {
          "$ref": "#/$defs/RuleAction",
          "default": "block"
        },
        "args": {
          "default": [],
          "description": "Arguments that must appear, in order, at the head of the typed\narguments for the rule to fire. Empty matches any arguments. `*`\nmatches any run of characters, `/` included, within one argument; an\nentry that is exactly `\"**\"` matches zero or more whole arguments. A\nleading `\"**\"` also skips a script path, so `[\"**\", \"*nitro\", \"dev\"]`\nfires on `node server.js --name nitro dev` too.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": true,
          "description": "`false` turns off a rule a parent layer declared.",
          "type": "boolean"
        },
        "programs": {
          "default": [],
          "description": "Program names this rule refuses, matched against the segment's command\nword by basename. An empty list matches nothing, which is how a child\nlayer exempts a subtree from a rule its parent declared.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reason": {
          "default": "",
          "description": "Shown to the agent verbatim when the rule denies. Name the replacement\ncommand; the agent retries from this text.",
          "type": "string"
        },
        "severity": {
          "$ref": "#/$defs/Severity",
          "default": "error"
        }
      },
      "type": "object"
    },
    "ContextConfig": {
      "description": "Files injected into an agent's context when the condition on them holds.\n\n```toml\n[[context.files]]\npath = \"crates/devkit-ports/AGENTS.md\"\n\n[[context.files]]\npath = \"docs/deploy.md\"\nwhen = { harness = [\"codex\"], env = { DEPLOY_TARGET = \"staging\" } }\n```\n\nAn entry is an array element, so a deeper `devkit.toml` replaces the whole\nlist rather than appending to it, the same as `[hooks]`.",
      "properties": {
        "files": {
          "default": [],
          "items": {
            "$ref": "#/$defs/ContextFile"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ContextFile": {
      "description": "One injected file.",
      "properties": {
        "path": {
          "description": "The file to inject, relative to the directory of the `devkit.toml` that\ndeclared it.",
          "type": "string"
        },
        "when": {
          "anyOf": [
            {
              "$ref": "#/$defs/FileCondition"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When to inject it. Absent means: when the edited target is under this\nfile's own directory."
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    },
    "DaemonConfig": {
      "description": "The `devkitd` supervisor: whether it starts, how long it lingers, and the\ncrash-loop, memory, and health policies it applies to supervised servers.\n\n```toml\n[daemon]\nenabled         = true\nmemory_warn_mb  = 1024\nmemory_limit_mb = 2048   # soft: SIGTERM, then the crash path respawns\nmemory_action   = \"restart\"\nmemory_max_mb   = 4096   # hard: the kernel OOM-kills the tree\n```\n\nThe ordering in that example is the one to keep: the soft action is the\ngraceful first responder, and the kernel cap is the backstop for a spike\ntoo fast for the poll loop.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Run gate: autostart the daemon only when true (or via DEVKIT_DAEMON=1 /\n--supervise).",
          "type": "boolean"
        },
        "health_fail_threshold": {
          "default": 3,
          "description": "Consecutive post-arming probe failures before a server is judged hung.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "health_probe_secs": {
          "default": 0,
          "description": "Health-probe interval in seconds; 0 disables probing (no probe thread).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "idle_timeout_secs": {
          "default": 1800,
          "description": "Exit after this many idle seconds with zero clients AND zero supervised\nchildren.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_restarts": {
          "default": 5,
          "description": "Crash-loop guard: restarts allowed within `restart_window_secs`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "memory_action": {
          "default": "warn",
          "description": "Action when tree-RSS crosses `memory_limit_mb`: \"warn\" (log a line) or\n\"restart\" (SIGTERM and let the crash path respawn). Any other value\nfalls back to warn.",
          "type": "string"
        },
        "memory_limit_mb": {
          "default": 0,
          "description": "Take `memory_action` past this tree-RSS in MB (0 = off).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "memory_limit_ticks": {
          "default": 3,
          "description": "Consecutive supervision ticks at or over `memory_limit_mb` before the\nrestart action fires (debounces transient allocation spikes).",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "memory_max_mb": {
          "default": 0,
          "description": "Hard kernel memory ceiling per supervised tree, in MB (0 = off,\nLinux-only). Enforced via a cgroup-v2 leaf with memory.max; a breach\nOOM-kills the tree and the crash path respawns it. Set above\nmemory_limit_mb so the soft poll-based action stays the graceful first\nresponder. Falls back to the soft action where cgroup-v2 delegation is\nunavailable.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "memory_warn_mb": {
          "default": 0,
          "description": "Log a loud line past this supervised tree-RSS in MB (0 = off).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "restart_window_secs": {
          "default": 60,
          "description": "Length of the sliding window the `max_restarts` budget is counted over.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "Defaults": {
      "description": "Project-wide paths and branch conventions. Each key is independently\noptional and empty (or, for `apps_dir`/`pr_base`/`stray_scan_width`, its own\ndocumented default) when unset; an empty path or branch key is not\nvalidated at config load, and what happens when it is used is up to the\nfeature that reads it.\n\nA relative path anchors to the layer that declared it, so a project can\ncommit this table as it stands and have it hold on every machine:\n\n```toml\n[defaults]\nworktree_root = \"../myproject_worktrees\"\nbaseline_dir  = \"../myproject_worktrees/_baselines\"\nbaseline_ref  = \"origin/main\"\n```\n\nOnly `branch_prefix` is personal. Put it in `devkit.local.toml`, or write\n`\"${USER}/\"`.",
      "properties": {
        "apps_dir": {
          "default": "apps",
          "description": "Repo-relative directory apps live under (e.g. \"apps\"). Used to infer app\npaths from doppler.yaml and to detect changed apps in a diff.",
          "type": "string"
        },
        "baseline_dir": {
          "default": "",
          "description": "Directory baseline worktrees are created under, one per fork-point\ncommit. `~` is expanded. Names a location on this machine, so a relative\nvalue anchors to the directory of the layer that declared it. Defaults\nto `_baselines` under `worktree_root`.",
          "type": "string"
        },
        "baseline_ref": {
          "default": "",
          "description": "Git ref the baseline server tracks, e.g. `origin/staging`.",
          "type": "string"
        },
        "branch_prefix": {
          "default": "",
          "description": "Prefix on branches created by `issue setup`, e.g. `you/`.",
          "type": "string"
        },
        "doppler_yaml": {
          "default": "",
          "description": "Path to the repo's `doppler.yaml`; its `setup` paths seed app path\ninference. `~` is expanded. Names a file inside the repository being\nworked on, so a relative value anchors to the checkout reading the\nconfig rather than to the layer that declared it — each worktree\nresolves its own copy. Leave empty and every app needs its own\n`path`.",
          "type": "string"
        },
        "ignored_checks": {
          "default": [],
          "description": "Glob patterns for status-check names to discount from a PR's CHECK\nverdict — e.g. a deploy left red by an unfinished PR. Matched\ncase-insensitively against each check's name; a PR reads green when only\nignored checks fail, and the ignored failures are still surfaced in the\ntriage output rather than hidden.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "issue_summary": {
          "default": false,
          "description": "Write the issue summary file on every `issue setup`, as though\n`--summary` were passed. `--summary` / `--no-summary` still win for one\nrun. Off by default.",
          "type": "boolean"
        },
        "pr_base": {
          "default": "main",
          "description": "Base branch used when opening PRs (e.g. \"main\", \"staging\").",
          "type": "string"
        },
        "pr_create_state": {
          "$ref": "#/$defs/PrCreateState",
          "default": "draft",
          "description": "State `issue pr create` opens a PR in when neither `--draft` nor\n`--ready` is given. Draft by default, so a new PR never lands in\nanyone's review queue until it is marked ready."
        },
        "require_pr_reviewer": {
          "default": false,
          "description": "Refuse any run that would leave a PR ready for review with no human\nGitHub reviewer other than its own author: `issue pr create --ready`,\n`issue pr ready`, and the draft-to-ready flip in `issue review request`.\nOff by default.",
          "type": "boolean"
        },
        "stray_scan_width": {
          "default": 64,
          "description": "Width of each app's port-band scan window for stray detection:\nports `[base_port, base_port + stray_scan_width)`. Default 64.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "worktree_include": {
          "default": [],
          "description": "Glob patterns (relative to the primary checkout's root) for untracked\nlocal files to copy into a newly created worktree. Each match is copied\nto the same relative path; a match that is a directory (or a pattern\nending in `/`) is copied recursively. Existing destinations are left\nalone, and only `issue sync-includes --overwrite` replaces them. Empty\nby default — the backfill is opt-in.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "worktree_root": {
          "default": "",
          "description": "Directory issue worktrees are created under. `~` is expanded. Names a\nlocation on this machine, so a relative value anchors to the\ndirectory of the config layer that declared it, even when that layer\nis the repository's main checkout.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "DocsManifest": {
      "description": "Libraries whose source is checked out at the version this project resolves.\nA project's `[docs]` section overlays the global `docs.toml` entry by entry.\n\n```toml\n[[docs.libs]]\nname  = \"minijinja\"\nnotes = \"template syntax for [tasks] and [templates]\"\n\n[[docs.libs]]\nname      = \"node-types\"\necosystem = \"js\"\npackage   = \"@types/node\"\n```\n\nThe first entry names nothing but the library, so its ecosystem and version\nboth come from the project's own lockfiles. The second overrides the\nregistry name, which is the only thing `name` cannot also serve as.",
      "properties": {
        "libs": {
          "default": [],
          "description": "One entry per managed library. Every field except `name` is optional so\nan overlay can override a single field of a global entry.",
          "items": {
            "$ref": "#/$defs/LibEntry"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "Ecosystem": {
      "description": "Which importer graph resolves a library's version, and therefore which\nlockfile is consulted.",
      "oneOf": [
        {
          "enum": [
            "rust",
            "js",
            "python"
          ],
          "type": "string"
        },
        {
          "const": "git",
          "description": "No package registry or lockfile; pinned by `ref` or default branch.",
          "type": "string"
        }
      ]
    },
    "Fidelity": {
      "description": "What a logged command carries. Ordered least to most revealing, so\nresolving across config layers is `min` and no layer can raise it.",
      "oneOf": [
        {
          "const": "hashed",
          "description": "A stable digest and nothing else.",
          "type": "string"
        },
        {
          "const": "redacted",
          "description": "The text, with known token shapes substituted by kind.",
          "type": "string"
        },
        {
          "const": "full",
          "description": "The text verbatim.",
          "type": "string"
        }
      ]
    },
    "FileCondition": {
      "description": "Conditions on an injected file. Every condition present must hold.",
      "properties": {
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Environment variables in the hook process. An empty value means\n\"present with any value\".",
          "type": "object"
        },
        "harness": {
          "default": [],
          "description": "Harness names: `claude-code`, `codex`, `cursor`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "path": {
          "default": null,
          "description": "A glob over the repo-relative targets of the tool call.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "GithubConfig": {
      "additionalProperties": false,
      "description": "Which GitHub repositories this project uses. Both default to the `origin`\nremote. They are separate because a fork opens its PRs upstream while its\nissues may sit on either side, and because a project may track issues in a\nrepository separate from its code.\n\nThis table is not under `[tracker]`: a project on Linear with a fork\nworkflow needs `pr_repo` just as much as a GitHub one does.\n\n```toml\n[github]\nissues_repo = \"org/planning\"\npr_repo     = \"upstream/app\"\n```",
      "properties": {
        "issues_repo": {
          "default": null,
          "description": "Repository holding the issues, e.g. `org/planning`.",
          "type": [
            "string",
            "null"
          ]
        },
        "pr_repo": {
          "default": null,
          "description": "Repository pull requests are opened against, e.g. `upstream/app`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "HarnessSection": {
      "description": "The `[harness]` table of a checkout's `devkit.toml`.\n\nThis is the shape `devkit schema` renders. Nothing at runtime deserializes\nthe table through it: the probe reads each key independently so one bad key\ncannot take the others down with it.\n\n```toml\n[harness]\nenforce_writes   = true\nenforce_commands = true\nscript_files     = \"warn\"\n```\n\nThe same two lines in `~/.config/devkit/config.toml` enforce across every\ncheckout, with no per-project file at all.",
      "properties": {
        "app_match": {
          "$ref": "#/$defs/AppMatch",
          "description": "How the guard resolves a guarded command to one of `[apps]`."
        },
        "commands": {
          "additionalProperties": {
            "$ref": "#/$defs/CommandRule"
          },
          "description": "Extra refusals beyond the ones devkit derives from `[apps]` and\n`[tasks]`. Merged across config layers like every other table.",
          "type": "object"
        },
        "enforce_commands": {
          "default": false,
          "description": "Refuse shell commands devkit already has a wired-up path for.",
          "type": "boolean"
        },
        "enforce_writes": {
          "default": false,
          "description": "Refuse writes to paths this checkout has not claimed with `lockm`.",
          "type": "boolean"
        },
        "log": {
          "$ref": "#/$defs/LogSection",
          "default": {
            "auto_prune": null,
            "command": null,
            "dir": null,
            "enabled": null,
            "max_age_days": null,
            "max_bytes": null,
            "prompt": null
          },
          "description": "What devkit records about the events a harness sends it."
        },
        "script_files": {
          "$ref": "#/$defs/PolicyAction",
          "default": "allow",
          "description": "A call to a stored script, whose contents are not read."
        },
        "shell": {
          "$ref": "#/$defs/ShellSetting",
          "default": "auto",
          "description": "The shell a hook command is read in."
        },
        "unresolved_writes": {
          "$ref": "#/$defs/PolicyAction",
          "default": "block",
          "description": "A write whose target could not be determined."
        },
        "unsupported_language": {
          "$ref": "#/$defs/PolicyAction",
          "default": "block",
          "description": "Executable source in a language devkit cannot analyze."
        }
      },
      "type": "object"
    },
    "HooksConfig": {
      "description": "Commands run on a devkit lifecycle event. Most keys are named\n`{before,after}_<event>` after the state change rather than the command\nthat reached it; `after_end` is the exception and names its run, because a\nrun-level event has exactly one caller and no worktree state to be named\nfor. Each key holds a list of argv arrays — no shell, so pipes, `&&`, and\nglobs are not available. A hook that fails to render, spawn, or exit zero\nwarns on stderr and the remaining hooks still run.\n\n```toml\n[hooks]\nafter_worktree_create = [[\"zoxide\", \"add\", \"{{ worktree }}\"]]\nafter_worktree_remove = [[\"zoxide\", \"remove\", \"{{ worktree }}\"]]\nafter_end             = [[\"alacritree\", \"project\", \"refresh\"]]\n```\n\nEach key is an array, so a deeper `devkit.toml` replaces the whole list\nrather than appending to it.",
      "properties": {
        "after_end": {
          "default": [],
          "description": "Runs once at the end of an `issue end` run that removed at least one\nworktree, after every `after_worktree_remove` hook, in the main\nrepository root. A run-level event: it carries `removed` (the removed\nworktree paths, in the order they were confirmed), `count`, `prefix`,\n`worktree_root`, `primary`, and `[templates.variables]`, and none of\nthe single-worktree keys.",
          "items": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "type": "array"
        },
        "after_worktree_create": {
          "default": [],
          "description": "Runs once in the root of a worktree `issue setup` or\n`issue pr checkout` has just created, after its apps are prepared and\nafter the command has reported the worktree. Each argv element is\nrendered as minijinja over `worktree`, `branch`, `issue`, `slug`,\n`apps`, `prefix`, and `[templates.variables]`.",
          "items": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "type": "array"
        },
        "after_worktree_remove": {
          "default": [],
          "description": "Runs once per worktree `issue end` removed, after every removal in the\nrun has finished and the stale worktree entries are pruned. The\nworktree is gone by then, so these run in the main repository root;\n`issue`, `slug` and `apps` come from the `.devkit/issue.toml` record\nread before the removal. Rendered over `worktree`, `branch`, `issue`,\n`slug`, `apps`, `prefix`, `worktree_root`, `primary`, and\n`[templates.variables]`.",
          "items": {
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "LayerMarker": {
      "description": "The `[config]` table. Read straight off the raw layer by `is_root_layer`\nrather than through `Config`, because it decides which layers exist before\nthere is a merged config to deserialize; this type exists so the published\nJSON Schema can still describe it.\n\n```toml\n[config]\nroot = true\n```",
      "properties": {
        "root": {
          "default": false,
          "description": "Stop walking upward at this file, and drop\n`~/.config/devkit/config.toml` from the layer stack.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "LibEntry": {
      "description": "One managed library. Every field except `name` is optional so a project\noverlay entry can override a single field of a global entry.",
      "properties": {
        "docs_dir": {
          "description": "Docs directory inside the checkout, overriding layout detection.",
          "type": [
            "string",
            "null"
          ]
        },
        "ecosystem": {
          "anyOf": [
            {
              "$ref": "#/$defs/Ecosystem"
            },
            {
              "type": "null"
            }
          ],
          "description": "Which importer graph resolves the version. Omit to detect it from the\nproject's lockfiles."
        },
        "name": {
          "description": "Id this library is addressed by on the `docm` command line, and the\nkey an overlay entry merges onto.",
          "type": "string"
        },
        "notes": {
          "description": "Freeform note surfaced by `docm info` and `docm list` — what this\nlibrary is here for.",
          "type": [
            "string",
            "null"
          ]
        },
        "package": {
          "description": "Registry package name when it differs from `name` (e.g. `@types/node`).",
          "type": [
            "string",
            "null"
          ]
        },
        "ref": {
          "description": "Manual pin (tag/branch/sha); wins over lockfile resolution.",
          "type": [
            "string",
            "null"
          ]
        },
        "repo": {
          "description": "Git URL to clone, skipping the registry lookup that would find it.",
          "type": [
            "string",
            "null"
          ]
        },
        "src_dir": {
          "description": "Source directory inside the checkout, overriding layout detection.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "name"
      ],
      "type": "object"
    },
    "LinearConfig": {
      "description": "Linear lookups that cost an extra API round trip, so each is opt-in.\n\n```toml\n[linear]\nresolve_pr_links = true\n```\n\nThe lookup authenticates with `LINEAR_API_KEY` from the environment or\n`~/.config/devkit/secrets.toml`; no token lives in this table.",
      "properties": {
        "resolve_pr_links": {
          "default": false,
          "description": "Query Linear for the issues linked to each PR in `issue prs` (one\nextra batched round trip per run). Off by default.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "LogSection": {
      "description": "Harness logging: what agents tried to run, and what devkit decided about\nit. Off unless the global config turns it on.\n\n`enabled = true`, `dir`, `auto_prune`, `max_age_days` and `max_bytes` are\nread from `~/.config/devkit/config.toml` alone and ignored wherever else\nthey appear. A project layer may do exactly two things: set\n`enabled = false`, and lower `command` or `prompt`. Everything a project\nlayer can do tightens. `dir` is on that list for the same reason\n`enabled = true` is: a project layer setting `dir = \"./.logs\"` would land\ncommand text inside the checkout.\n\n```toml\n[harness.log]\nenabled      = true              # global config only\ncommand      = \"redacted\"        # full | redacted | hashed\nprompt       = \"off\"             # off | hashed | redacted | full\nauto_prune   = true              # global config only\ndir          = \"${HOME}/logs/devkit\" # global only; defaults under state_dir\nmax_age_days = 30                # global only; absent means unlimited\nmax_bytes    = 2000000000        # global only; absent means unlimited\n```\n\nEvery leaf is `Option` so a layer that set nothing is distinguishable from\none that set the default, which is what the downward clamp needs.",
      "properties": {
        "auto_prune": {
          "default": null,
          "description": "Sweep the log directory at session end. Global config only.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "command": {
          "anyOf": [
            {
              "$ref": "#/$defs/Fidelity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How much of a command's text a record carries."
        },
        "dir": {
          "default": null,
          "description": "Where records land. Global config only; defaults under the state dir.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": null,
          "description": "Turn logging on. Read from the global config alone.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "max_age_days": {
          "default": null,
          "description": "Days of records to keep. Global config only; absent means unlimited.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_bytes": {
          "default": null,
          "description": "Bytes of records to keep. Global config only; absent means unlimited.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "prompt": {
          "anyOf": [
            {
              "$ref": "#/$defs/PromptFidelity"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How much of a submitted prompt a record carries."
        }
      },
      "type": "object"
    },
    "ParallelismConfig": {
      "description": "Width of the shared worker pool. Machine tuning rather than a project\nconvention, so it belongs in the personal layer at\n`~/.config/devkit/config.toml` rather than a repository's `devkit.toml`.\n\n```toml\n[parallelism]\nthreads = 8\n```",
      "properties": {
        "threads": {
          "default": null,
          "description": "Threads in the pool devkit shares across its parallel work. The\n`DEVKIT_THREADS` environment variable wins over this; leaving it unset\ntakes the pool's own default. Zero is refused rather than clamped,\nbecause rayon reads a zero thread count as one thread per core.",
          "format": "uint",
          "minimum": 1,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "Person": {
      "description": "A team member's handle mapping (Slack user-id, GitHub login, etc.).\n\n```toml\n[people.alice]\nslack  = \"U0XXXXXXXXX\"\ngithub = \"alice-gh\"\n```",
      "properties": {
        "github": {
          "default": null,
          "description": "GitHub login requested as the PR reviewer when this alias is passed to\n`issue review request --to`. Omit and the alias only gets Slacked.",
          "type": [
            "string",
            "null"
          ]
        },
        "slack": {
          "description": "Slack user or channel id, e.g. `U0XXXXXXXXX`.",
          "type": "string"
        }
      },
      "required": [
        "slack"
      ],
      "type": "object"
    },
    "PolicyAction": {
      "description": "What a policy finding does to the tool call.",
      "oneOf": [
        {
          "const": "block",
          "description": "Deny the tool call with an actionable reason.",
          "type": "string"
        },
        {
          "const": "warn",
          "description": "Allow it and tell the agent what was not checked.",
          "type": "string"
        },
        {
          "const": "allow",
          "description": "Allow it silently.",
          "type": "string"
        }
      ]
    },
    "PrCreateState": {
      "description": "The state a PR is opened in by `issue pr create` when neither `--draft` nor\n`--ready` is passed.",
      "oneOf": [
        {
          "const": "draft",
          "description": "Opened as a draft. Reviewers are notified only once it is marked ready.",
          "type": "string"
        },
        {
          "const": "ready",
          "description": "Opened ready for review.",
          "type": "string"
        }
      ]
    },
    "PrepFile": {
      "description": "A file written into an app's directory during `issue setup`, before the\napp's `setup` commands run. `content` is written verbatim — no format\nassembly or newline injection. Parent directories are created. Existing\nfiles are left untouched unless `overwrite` is set.\n\n```toml\n[apps.web]\nbase_port = 4100\nlaunch    = [\"next\", \"dev\", \"-p\", \"{{ port }}\"]\n\n[[apps.web.prep_files]]\npath    = \".env.local\"\ncontent = \"\"\"\nSOME_FEATURE_FLAG=dummy\n\"\"\"\n```",
      "properties": {
        "content": {
          "description": "File contents, written byte-for-byte.",
          "type": "string"
        },
        "overwrite": {
          "default": false,
          "description": "Overwrite an existing file rather than skipping it.",
          "type": "boolean"
        },
        "path": {
          "description": "Target path, relative to the app's directory.",
          "type": "string"
        }
      },
      "required": [
        "path",
        "content"
      ],
      "type": "object"
    },
    "PreserveConfig": {
      "additionalProperties": false,
      "description": "Files copied out of a worktree before `issue end` removes it. Each entry\nnames its own destination, so one run can archive different files to\ndifferent places. Rendering, path rules, and the fail-open contract are in\n`docs/configuration.md`.\n\n`deny_unknown_fields` because a misspelled `required` would otherwise be\nconsumed silently, leaving the entry fail-open while the user believes the\nfiles are protected.\n\n```toml\n[preserve.scratch]\nfrom     = [\".scratch/\"]\nto       = \"{{ worktree_root }}/archive/{{ issue }}/scratch\"\nrequired = true\n\n[preserve.notes]\nfrom = [\"docs/notes/*.md\"]\nto   = \"{{ primary }}/.devkit/archive/{{ issue }}\"\n```\n\n`scratch` is `required`, so a failure there keeps the worktree, its branch\nand its summary instead of removing them. `notes` warns and the worktree\ngoes.",
      "properties": {
        "from": {
          "description": "Glob patterns for the files to copy, relative to the worktree root and\nrendered as minijinja. A pattern that renders empty is skipped; one that\nmatches nothing is not a failure.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "required": {
          "default": false,
          "description": "Keep the worktree instead of removing it when this entry warns.",
          "type": "boolean"
        },
        "to": {
          "description": "Destination directory, rendered as minijinja. Must render to a non-empty\nabsolute path. It is created when the first file lands in it, so an\nentry that matches nothing leaves no directory behind.",
          "type": "string"
        }
      },
      "required": [
        "from",
        "to"
      ],
      "type": "object"
    },
    "PromptFidelity": {
      "description": "What a logged prompt carries. Same ordering rule as [`Fidelity`], with an\n`off` below every level of it: a corpus can carry full command text without\ncarrying what the human typed.",
      "oneOf": [
        {
          "enum": [
            "hashed",
            "redacted",
            "full"
          ],
          "type": "string"
        },
        {
          "const": "off",
          "description": "Record that a prompt was submitted, and none of its text.",
          "type": "string"
        }
      ]
    },
    "Required": {
      "description": "Who must supply an `--arg` even though a default exists. `Never` is the\nabsence of a marking, not a licence to omit: the derived rule (an arg with\nno default is required) is a floor this enum sits on top of and cannot\nlower. `Never` is therefore meaningful only as a task-level override of a\nvariable-level marking.",
      "enum": [
        "never",
        "humans",
        "agents",
        "always"
      ],
      "type": "string"
    },
    "RuleAction": {
      "description": "What a matching command rule does. A rule that should not act is disabled\nwith `enabled = false`, so there is no `allow`.",
      "enum": [
        "block",
        "warn"
      ],
      "type": "string"
    },
    "RulesConfig": {
      "description": "Rule injection settings.\n\n```toml\n[rules]\nenabled = true\nmin_severity = \"should\"\nper_event_limit = 5\n```",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Rule and file injection as a whole.",
          "type": "boolean"
        },
        "index": {
          "default": null,
          "description": "Where the rule index lives. Absent means the path the extractor writes.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_event_bytes": {
          "default": 65536,
          "description": "The rendered total for one event is truncated to this.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_file_bytes": {
          "default": 16384,
          "description": "A single injected file larger than this is skipped.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "min_severity": {
          "default": "should",
          "description": "The least severe rule still injected: `must`, `should` or `can`.",
          "type": "string"
        },
        "per_event_limit": {
          "default": 5,
          "description": "How many rules and files one event may inject.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "RunArg": {
      "anyOf": [
        {
          "description": "Renders to exactly one argument, whatever the result contains.",
          "type": "string"
        },
        {
          "additionalProperties": false,
          "description": "Renders, then splits on `on` into a run of separate arguments. A\ntemplate rendering empty contributes no arguments at all.",
          "properties": {
            "on": {
              "type": "string"
            },
            "split": {
              "type": "string"
            }
          },
          "required": [
            "split",
            "on"
          ],
          "type": "object"
        }
      ],
      "description": "One entry of a task's `run`, each rendering exactly one minijinja template\nand differing only in what it does with the result.\n\n`deny_unknown_fields` because `Split` is matched untagged: without it a\nmisspelled key alongside a well-formed `split`/`on` pair deserializes\nsilently, and the typo changes nothing the author can see.\n\n```toml\n[tasks.stage]\nrun = [\"git\", \"add\", \"--\", { split = \"{{ files }}\", on = \";\" }]\n```\n\n`devrun task stage --arg 'files=new file.txt;other.txt'` reaches `git` as\nfour arguments, the space in the first filename included, because devkit\nexecs the argv rather than handing it to a shell."
    },
    "Severity": {
      "description": "How a diagnostic is classified for the agent.",
      "enum": [
        "info",
        "warning",
        "error"
      ],
      "type": "string"
    },
    "ShellSetting": {
      "description": "The shell whose syntax a hook command is read in. `auto` resolves from\nthe tool name and the harness; see `docs/configuration.md`.",
      "enum": [
        "auto",
        "bash",
        "powershell"
      ],
      "type": "string"
    },
    "Step": {
      "description": "One step of a sequence task: run a sibling command task, or bring an app up.\n\n```toml\n[tasks.stack]\nsteps = [{ task = \"api-build\" }, { up = \"api\" }]\n```",
      "oneOf": [
        {
          "additionalProperties": false,
          "description": "Name of a sibling command task to run at this point in the sequence.",
          "properties": {
            "task": {
              "type": "string"
            }
          },
          "required": [
            "task"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "description": "Name of an app to bring up. Idempotent: an app already running in this\nworktree is reported rather than spawned again.",
          "properties": {
            "up": {
              "type": "string"
            }
          },
          "required": [
            "up"
          ],
          "type": "object"
        }
      ]
    },
    "TaskConfig": {
      "description": "A canned oneshot invoked by name via `devrun task`: either a command\n(`run`, optionally scoped to an `app` for cwd + static_env) or a sequence\n(`steps`). Exactly one of `run`/`steps` must be set; a sequence task\ncarries no `app`/`env`. Validated at resolution, not at parse.\n\n```toml\n[tasks.api-prod-build]\ndescription = \"prod nitro build (node-server preset)\"\napp = \"api-prod\"        # run in this app's dir, inherit its static_env\nrun = [\"doppler\", \"run\", \"-c\", \"dev_local\",\n       \"--preserve-env=NITRO_PRESET\", \"--\", \"bun\", \"nitro\", \"build\"]\nenv = { NITRO_PRESET = \"node-server\" }\n\n[tasks.commit]\nrun = [\"git\", \"commit\", \"-m\", \"{% if issue is defined %}{{ issue }}: {% endif %}{{ msg }}\"]\nguard = true            # redirect an agent's own `git commit` here\n\n[tasks.profile-lab-os]\ndescription = \"prod api + profiled lab-os, wired together\"\nsteps = [\n  { task = \"api-prod-build\" },\n  { up = \"api-prod\" },\n]\n```\n\n`msg` is an arg of the `commit` task: no `[templates.variables]` entry\nsupplies it, so `devrun task commit --arg msg=...` is required.",
      "properties": {
        "app": {
          "default": null,
          "description": "App whose directory the task runs in, inheriting that app's\n`static_env`. Omit to run at the repo root.",
          "type": [
            "string",
            "null"
          ]
        },
        "description": {
          "default": null,
          "description": "One line shown in `devrun task --list` and the session brief.",
          "type": [
            "string",
            "null"
          ]
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Env vars set for this task, layered over the app's `static_env` and\nunder the CLI's `--env-file` and `--env`.",
          "type": "object"
        },
        "guard": {
          "default": null,
          "description": "Force the command guard's decision for this task instead of deriving it.\n`true` always redirects a matching command to `devrun task <name>`;\n`false` never does. Absent derives from whether devkit changes the\nprocess at all.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "require_live": {
          "default": [],
          "description": "Apps whose server must be live in this worktree when the task executes.\nEach name must be referenced by the task's templates via `ports[...]`;\na user `--env` override of every referencing value waives the check.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "required_args": {
          "additionalProperties": {
            "$ref": "#/$defs/Required"
          },
          "default": {},
          "description": "Args this task requires beyond what `[templates.variables]` markings\nsay, as name to marking. A task entry beats a variable entry, including\nrelaxing one to `never`. Neither can lower the derived floor: an arg\nwith no default is required whatever this says. Each name must be an\narg the task reads.",
          "type": "object"
        },
        "run": {
          "default": [],
          "description": "The command as one argv (program + args), every entry a minijinja\ntemplate over `{{ port }}`, `ports['<app>']`, and\n`[templates.variables]`. A `{ split = \"...\", on = \"...\" }` entry spreads\nits rendered value into separate arguments. The program must be a plain\nstring. Mutually exclusive with `steps`.",
          "items": {
            "$ref": "#/$defs/RunArg"
          },
          "type": "array"
        },
        "steps": {
          "default": [],
          "description": "A sequence run in order, each step either a command or an `up`. Each\nstep re-resolves its ports immediately before it runs. Mutually\nexclusive with `run`.",
          "items": {
            "$ref": "#/$defs/Step"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "Templates": {
      "description": "Config-driven minijinja templates for the issue-lifecycle strings. Each\n`None` field falls back to its `DEFAULT_*` constant, which reproduces the\nhistorical hardcoded output. `variables` are user constants merged under\nevery render context.\n\n```toml\n[templates]\nbranch             = \"{{ prefix }}{{ issue }}-{{ slug }}\"\nworktree_dir       = \"{{ slug }}\"\npr_title           = \"{{ issue }}: {{ input }}\"\npr_body            = \"Closes {{ issue }}.\\n\\n{{ input }}\"\nissue_summary_path = \"{{ worktree }}/.devkit/issue.md\"\n\n[templates.variables]\nteam = \"platform\"\n```\n\n`issue_summary_path` renders `{{ worktree }}` here, which keeps the summary\ninside the worktree where a `[preserve]` pattern can name it; left relative\nit is taken from `defaults.worktree_root` instead.",
      "properties": {
        "branch": {
          "description": "Branch name created by `issue setup`. Context: `prefix`, `issue`,\n`slug`, `apps`. Defaults to `{{ prefix }}{{ slug }}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "branch_max": {
          "description": "Longest branch `issue setup` will render. A derived slug is shortened on\na word boundary to fit. A template whose fixed text already fills this\nyields the shortest slug still worth reading rather than an error, since\na git ref has no hard length limit. Defaults to 46.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "checkout_worktree_dir": {
          "description": "Worktree directory name created by `issue pr checkout`. Context:\n`pr_number`, `pr_title`, `linear_id`, `linear_title` — titles are\nslugified, and the `linear_*` names are historical: they carry\nwhichever tracker's id and title resolved, and are empty on the\nPR-only path.",
          "type": [
            "string",
            "null"
          ]
        },
        "checkout_worktree_dir_max": {
          "description": "Longest worktree directory name `issue pr checkout` will render.\n`pr_title` and `linear_title` are shortened to fit, splitting the budget\nwhen a template renders both. A template whose fixed text already fills\nthis is an error. Defaults to 46.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "issue_summary": {
          "description": "Body of the file `issue setup --summary` writes. Context: `issue`,\n`title`, `url`, `description`, `state`, `assignee`, `priority`,\n`estimate`, `labels`, `parent`, `project`, `worktree`, `branch`, `slug`,\n`prefix`, `apps` — every Linear field is the empty string when Linear\nhas nothing there.",
          "type": [
            "string",
            "null"
          ]
        },
        "issue_summary_path": {
          "description": "Where `issue setup --summary` writes the issue summary file. A relative\npath is taken from `defaults.worktree_root`, so the file outlives the\nworktree; render `{{ worktree }}` into it to keep it inside instead.\nContext: the `issue_summary` context below.",
          "type": [
            "string",
            "null"
          ]
        },
        "pr_body": {
          "description": "Body of a PR opened by `issue review request`. `{{ input }}` is the\n`--pr-body` argument.",
          "type": [
            "string",
            "null"
          ]
        },
        "pr_title": {
          "description": "Title of a PR opened by `issue review request`. `{{ input }}` is the\n`--pr-title` argument.",
          "type": [
            "string",
            "null"
          ]
        },
        "review_finish": {
          "description": "Slack message sent by `issue review finish`. Same context as\n`review_request`, plus `author`.",
          "type": [
            "string",
            "null"
          ]
        },
        "review_request": {
          "description": "Slack message sent by `issue review request`. Rendered once per\nrecipient with `name`, `slack_id`, `pr_url`, `pr_title`, and `input`.",
          "type": [
            "string",
            "null"
          ]
        },
        "variables": {
          "additionalProperties": {
            "$ref": "#/$defs/VariableDecl"
          },
          "default": {},
          "description": "Constants available to every template above. A context field of the same\nname wins, and `--arg key=value` overrides either. An entry may instead\nbe a table carrying a `default` and a `required` marking.",
          "type": "object"
        },
        "worktree_dir": {
          "description": "Worktree directory name created by `issue setup`, relative to\n`defaults.worktree_root`. Same context as `branch`; defaults to\n`{{ slug }}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "worktree_dir_max": {
          "description": "Longest worktree directory name `issue setup` will render from\n`{{ short_slug }}`. A template whose fixed text already fills this is an\nerror: a limit on a filesystem path that does not hold is worse than a\nsetup that stops. Has no effect on a `worktree_dir` that does not render\n`{{ short_slug }}`. Defaults to 24.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TrackerConfig": {
      "description": "The `[tracker]` table.\n\n```toml\n[tracker]\nkind = \"github\"\n```\n\nNaming the kind is what stops a `LINEAR_API_KEY` exported machine-wide from\nresolving Linear for a project that does not use it.",
      "properties": {
        "kind": {
          "anyOf": [
            {
              "$ref": "#/$defs/TrackerKind"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Force a tracker instead of detecting one."
        }
      },
      "type": "object"
    },
    "TrackerKind": {
      "description": "Which issue tracker a project uses. Absent means detect: a resolvable\n`LINEAR_API_KEY`, then a GitHub `origin` remote, then no tracker.",
      "enum": [
        "linear",
        "github",
        "none"
      ],
      "type": "string"
    },
    "VariableDecl": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "additionalProperties": false,
          "properties": {
            "default": {
              "type": [
                "string",
                "null"
              ]
            },
            "required": {
              "anyOf": [
                {
                  "$ref": "#/$defs/Required"
                },
                {
                  "type": "null"
                }
              ],
              "description": "`None` is a marking the author did not write, which is not the same\nas one written as `never`: an unmarked valueless entry is required\nby the derived rule, while `required = \"never\"` on the same entry\nasks to relax that and is refused at load."
            }
          },
          "type": "object"
        }
      ],
      "description": "One `[templates.variables]` entry: a bare string, or a table carrying a\n`default` and a `required` marking.\n\n`deny_unknown_fields` for the reason `RunArg` documents: under untagged\nmatching a misspelled key beside a well-formed pair deserializes silently,\nand the author sees no diagnostic while their guard does nothing.\n\n```toml\n[templates.variables]\nteam = \"platform\"                              # a constant, never required\nmsg = { default = \"wip\", required = \"agents\" } # defaulted, but agents must pass it\nticket = { required = \"always\" }               # declared, no default, always required\n```\n\n`default` is the fallback used when nothing else supplies the name, which\nis what these entries have always been: `template::render` merges them\nunderneath the context, and `--arg` overwrites them. Note that minijinja's\nown `default` filter is a different thing and does not affect whether an\narg is required."
    }
  },
  "$id": "https://github.com/AbysmalBiscuit/devkit/releases/latest/download/devkit-config.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Configuration for the devkit CLIs (devrun, portm, issue, lockm, docm, devkit).",
  "properties": {
    "apps": {
      "additionalProperties": {
        "$ref": "#/$defs/AppConfig"
      },
      "default": {},
      "description": "One table per runnable app, keyed by the app id passed to\n`issue setup --apps` and `devrun up`.",
      "type": "object"
    },
    "brief": {
      "$ref": "#/$defs/BriefConfig",
      "default": {
        "apps": true,
        "enabled": true,
        "locks": true,
        "pins": true,
        "tasks": true
      },
      "description": "Which sections `devkit brief` emits at session start."
    },
    "config": {
      "$ref": "#/$defs/LayerMarker",
      "default": {
        "root": false
      },
      "description": "Layer-stack control: `root = true` stops the upward walk here."
    },
    "context": {
      "$ref": "#/$defs/ContextConfig",
      "default": {
        "files": []
      },
      "description": "Files injected into an agent's context, each gated by its own\ncondition."
    },
    "daemon": {
      "$ref": "#/$defs/DaemonConfig",
      "default": {
        "enabled": false,
        "health_fail_threshold": 3,
        "health_probe_secs": 0,
        "idle_timeout_secs": 1800,
        "max_restarts": 5,
        "memory_action": "warn",
        "memory_limit_mb": 0,
        "memory_limit_ticks": 3,
        "memory_max_mb": 0,
        "memory_warn_mb": 0,
        "restart_window_secs": 60
      },
      "description": "The `devkitd` supervisor: autostart gate, crash-loop budget, and the\nmemory and health policies applied to supervised servers."
    },
    "defaults": {
      "$ref": "#/$defs/Defaults",
      "default": {
        "apps_dir": "apps",
        "baseline_dir": "",
        "baseline_ref": "",
        "branch_prefix": "",
        "doppler_yaml": "",
        "ignored_checks": [],
        "issue_summary": false,
        "pr_base": "main",
        "pr_create_state": "draft",
        "require_pr_reviewer": false,
        "stray_scan_width": 64,
        "worktree_include": [],
        "worktree_root": ""
      },
      "description": "Project-wide paths and branch conventions."
    },
    "docs": {
      "$ref": "#/$defs/DocsManifest",
      "default": {
        "libs": []
      },
      "description": "Libraries `docm` resolves version-correct checkouts for."
    },
    "github": {
      "$ref": "#/$defs/GithubConfig",
      "default": {
        "issues_repo": null,
        "pr_repo": null
      },
      "description": "Which GitHub repositories back issues and pull requests."
    },
    "harness": {
      "$ref": "#/$defs/HarnessSection",
      "description": "Write-enforcement opt-in for this checkout."
    },
    "hooks": {
      "$ref": "#/$defs/HooksConfig",
      "default": {
        "after_end": [],
        "after_worktree_create": [],
        "after_worktree_remove": []
      },
      "description": "Commands devkit runs when a lifecycle event fires, keyed\n`{before,after}_<event>`."
    },
    "linear": {
      "$ref": "#/$defs/LinearConfig",
      "default": {
        "resolve_pr_links": false
      },
      "description": "Linear lookups that cost an extra API round trip."
    },
    "parallelism": {
      "$ref": "#/$defs/ParallelismConfig",
      "default": {
        "threads": null
      },
      "description": "Width of the shared worker pool. Machine tuning; carries no project\nconvention, so a config may hold it alone."
    },
    "people": {
      "additionalProperties": {
        "$ref": "#/$defs/Person"
      },
      "default": {},
      "description": "Reviewer/recipient aliases, keyed by the short name passed to\n`issue review --to`.",
      "type": "object"
    },
    "preserve": {
      "additionalProperties": {
        "$ref": "#/$defs/PreserveConfig"
      },
      "default": {},
      "description": "Files copied out of an issue worktree before `issue end` removes it,\nkeyed by the name that labels the entry's progress step and its\nwarnings.",
      "type": "object"
    },
    "rules": {
      "$ref": "#/$defs/RulesConfig",
      "default": {
        "enabled": true,
        "index": null,
        "max_event_bytes": 65536,
        "max_file_bytes": 16384,
        "min_severity": "should",
        "per_event_limit": 5
      },
      "description": "Rule injection settings: enablement, severity floor, and the per-event\nsize budget."
    },
    "tasks": {
      "additionalProperties": {
        "$ref": "#/$defs/TaskConfig"
      },
      "default": {},
      "description": "Canned oneshot commands, keyed by the name passed to `devrun task`.",
      "type": "object"
    },
    "templates": {
      "$ref": "#/$defs/Templates",
      "default": {
        "branch": null,
        "branch_max": null,
        "checkout_worktree_dir": null,
        "checkout_worktree_dir_max": null,
        "issue_summary": null,
        "issue_summary_path": null,
        "pr_body": null,
        "pr_title": null,
        "review_finish": null,
        "review_request": null,
        "variables": {},
        "worktree_dir": null,
        "worktree_dir_max": null
      },
      "description": "Minijinja templates for the strings `issue setup` and `issue review`\ngenerate — branch names, worktree directories, PR fields, Slack bodies."
    },
    "tracker": {
      "$ref": "#/$defs/TrackerConfig",
      "default": {
        "kind": null
      },
      "description": "Which issue tracker backs `issue`. Detected when the table is absent."
    }
  },
  "title": "devkit.toml",
  "type": "object"
}
