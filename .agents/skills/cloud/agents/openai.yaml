interface:
  display_name: cloud
  short_description: Use when starting issue or feature work in a cloud agent session, or recovering that work after resume or compaction.
policy:
  allow_implicit_invocation: true
