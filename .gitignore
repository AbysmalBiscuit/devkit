/target
/.worktrees

# Personal/local devkit configs. The config that drives the engine is
# machine- and person-specific (paths, app catalog, Slack/GitHub handles,
# local secrets) and must never be distributed. Document the config *shape*
# in docs/ instead — see docs/configuration.md.
/configs/*.toml
devkit.local.toml
/AGENTS.local.md
/CLAUDE.local.md
/.agents/**/__pycache__/
/.agents/**/.venv/

# Cloud setup templates are source files.
!/.agents/skills/cloud/assets/devkit.local.toml
!/.agents/skills/cloud/assets/AGENTS.local.md
!/.agents/skills/cloud/assets/CLAUDE.local.md

# Subagent-driven-development progress ledger (local scratch)
/.superpowers/
